const PlaceholderPage = ({ title }) => (
  <div className="flex items-center justify-center h-full">
    <h1 className="text-2xl text-dark">{title} are rendered here</h1>
  </div>
);

export default PlaceholderPage; 